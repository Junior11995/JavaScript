// uso de operadores logicos 

let a = true;
let b = false;

// Uso de and o y
console.log(`${a} && ${b} -> ${a && b}`);

// Uso de or 
console.log(`${a} || ${b} -> ${a || b}`);

// Uso de not 
console.log(`${!a} ! ${b}`);